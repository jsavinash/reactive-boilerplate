//external import
import * as React from "react";

function Module() {
  // const data = useSelector((state) => state.module.data);
  return (
    <div className="module__heading">Module</div>
  );
}

export default Module;
